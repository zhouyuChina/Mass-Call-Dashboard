# API 配置
# API 基础地址
VITE_API_BASE_URL=http://47.237.31.83:7000/api

# WebSocket 配置（可选，默认从 API_BASE_URL 推导）
# VITE_WS_URL=ws://47.237.31.83:7000/ws

# 开发环境配置
# VITE_DEV_MODE=true

# Mock 数据配置
# 是否启用 mock 数据（开发环境默认启用，生产环境默认禁用）
# 设置为 'true' 强制启用，设置为 'false' 强制禁用
# 推荐：开发时设置为 true，后端联调时设置为 false
# VITE_ENABLE_MOCK=false

# 开发环境自动登录
# 开发环境默认启用自动登录，设置为 'false' 禁用
# 注意：自动登录现在独立于 mock 数据，可以在禁用 mock 时使用
# 推荐：开发时设置为 true 以跳过登录步骤
# VITE_DEV_AUTO_LOGIN=false
